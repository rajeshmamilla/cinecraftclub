spring:
  datasource:
    url: jdbc:postgresql://ep-young-flower-ap763nqx-pooler.c-7.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
    username: neondb_owner
    password: npg_7FDLOnc8CbAg
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    baseline-on-migrate: true

jwt:
  secret: ${JWT_SECRET:a-very-secure-secret-key-that-should-be-changed-in-production}
  expiration: 86400000 # 24 hours in ms
